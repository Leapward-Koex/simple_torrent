name: simple_torrent_platform_interface
description: Shared types, wire contract, and platform interface for the simple_torrent Flutter plugin.
version: 2.0.0
homepage: https://github.com/Leapward-Koex/simple_torrent
repository: https://github.com/Leapward-Koex/simple_torrent/tree/master/packages/simple_torrent_platform_interface
issue_tracker: https://github.com/Leapward-Koex/simple_torrent/issues
topics:
  - bittorrent
  - torrent
  - plugin-platform-interface

environment:
  sdk: '>=3.12.0 <4.0.0'
  flutter: '>=3.44.0'

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.1.8

dev_dependencies:
  flutter_test:
    sdk: flutter
