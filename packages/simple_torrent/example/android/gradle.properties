org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Flutter 3.47 keeps the legacy Android DSL and Kotlin Gradle plugin enabled
# while its AGP 9 migration support settles. These are Flutter template flags,
# not project-specific workarounds.
android.newDsl=false
android.builtInKotlin=false
